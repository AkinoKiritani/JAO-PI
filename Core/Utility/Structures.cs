namespace JAO_PI.Core.Utility
{
    class Structures
    {
        public enum Frames
        {
            MainFrame,
            SearchFrame,
            GoToFrame
        }
        public enum ContextMenuItems
        {
            Close,
            Rename,
            Save
        }
        public enum StatusBar
        {
            Line,
            Column
        }
    }
}
